/**
 * Builder.io settings
 * Replace YOUR_BUILDER_API_KEY below with your public API key:
 * - Get it from builder.io -> Account -> API keys -> Public API Key
 */
import { builder } from '@builder.io/react';

builder.init('YOUR_BUILDER_API_KEY');

export default builder;