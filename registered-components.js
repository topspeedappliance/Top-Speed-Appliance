/**
 * Register Builder.io components here.
 * Example:
 * import MyBanner from '../components/MyBanner';
 * builder.registerComponent(MyBanner, { name: 'MyBanner' });
 */
import { builder } from '@builder.io/react';

export function registerBuilderComponents(){
  // Register components exported from Builder or your custom components.
  // e.g. builder.registerComponent(MyComponent, { name: 'ServiceCard' });
}